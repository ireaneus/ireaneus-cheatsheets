# To get cheat working when getting docopt module errors
# Instead of running the python setup.py installer run the following
sudo pip install cheat

# This will upgrade the necessary docopt module and others
