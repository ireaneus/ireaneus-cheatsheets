# background 
bg %[n]

# foreground
fg %[n]

# jobs
jobs

# Ctrl+Z will suspend job
disown -h 	# disowns the background job
bg 1		# sends the job to the background
exit		# lets you exit without killing the background job
# Ctrl+C will kill job

# Close terminal window will close all background jobs
