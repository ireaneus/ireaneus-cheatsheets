# Puppet Commands Cheat Sheet

# list puppet modules loaded
puppet module list

# List puppet resources
puppet resource service

# list puppet describe objects include resources
puppet describe -l
puppet describe service | less

puppet agent --enable
puppet agent -t -v #Puppet Master 
