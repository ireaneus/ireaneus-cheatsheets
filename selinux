# Commands for SELINUX management

# chcon - changes context for files and directories
chcon -R -t usr_t directory1/	# recursively changes directory1 with type usr_t

ls -alZ				# Z will list selinux context

# permissive and enforcing
